services:
  api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./api:/usr/src/api:rw
    environment:
      TZ: Asia/Tokyo
    command: ["python3", "main.py"]
    restart: unless-stopped
    tty: true